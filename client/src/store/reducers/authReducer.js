/** @format */

const initState = { isLoggedIn: false, token: null };

const authReducer = (state = initState, action) => {
  switch (action.key) {
    default:
      return state;
  }
};
export default authReducer;
